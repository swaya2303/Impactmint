# name: CI/CD Pipeline

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# jobs:
#   backend-test:
#     name: Backend Tests
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [18.x, 20.x]

#     services:
#       mongodb:
#         image: mongo:6.0
#         ports:
#           - 27017:27017
#         options: >-
#           --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Setup Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: 'npm'
#           cache-dependency-path: backend/package-lock.json

#       - name: Install dependencies
#         working-directory: ./backend
#         run: npm ci

#       - name: Lint
#         working-directory: ./backend
#         run: npm run lint

#       - name: Run tests
#         working-directory: ./backend
#         run: npm test
#         env:
#           MONGODB_URI: mongodb://localhost:27017/impactmint_test
#           JWT_SECRET: test-secret
#           JWT_REFRESH_SECRET: test-refresh-secret
#           HEDERA_OPERATOR_ID: 0.0.12345
#           HEDERA_OPERATOR_KEY: 302e020100300506032b65700422042000000000000000000000000000000000

#       - name: Build
#         working-directory: ./backend
#         run: npm run build

#   frontend-test:
#     name: Frontend Tests
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [18.x, 20.x]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Setup Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         working-directory: ./frontend
#         run: npm ci

#       - name: Lint
#         working-directory: ./frontend
#         run: npm run lint || true

#       - name: Run tests
#         working-directory: ./frontend
#         run: npm test -- --watchAll=false || true

#       - name: Build
#         working-directory: ./frontend
#         run: npm run build

#   docker-build:
#     name: Docker Build
#     runs-on: ubuntu-latest
#     needs: [backend-test, frontend-test]
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Login to Docker Hub
#         uses: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and push backend
#         uses: docker/build-push-action@v4
#         with:
#           context: ./backend
#           file: ./backend/Dockerfile
#           push: true
#           tags: ${{ secrets.DOCKER_USERNAME }}/impactmint-backend:latest
#           cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/impactmint-backend:buildcache
#           cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/impactmint-backend:buildcache,mode=max

#       - name: Build and push frontend
#         uses: docker/build-push-action@v4
#         with:
#           context: ./frontend
#           file: ./frontend/Dockerfile
#           push: true
#           tags: ${{ secrets.DOCKER_USERNAME }}/impactmint-frontend:latest
#           cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/impactmint-frontend:buildcache
#           cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/impactmint-frontend:buildcache,mode=max

#   deploy:
#     name: Deploy to Production
#     runs-on: ubuntu-latest
#     needs: [docker-build]
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Deploy to server
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.DEPLOY_HOST }}
#           username: ${{ secrets.DEPLOY_USER }}
#           key: ${{ secrets.DEPLOY_SSH_KEY }}
#           script: |
#             cd /var/www/impactmint
#             docker-compose pull
#             docker-compose up -d
#             docker-compose exec -T backend npm run migrate || true
